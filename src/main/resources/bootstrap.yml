spring:
  application:
    name: import-customer-service
  cloud:
    config:
      uri: http://config-service:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: crm
